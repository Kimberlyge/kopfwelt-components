const gulp = require('gulp');
const shell = require('gulp-shell');

// Runs the `templates` task two times, one for each language.
// gulp.task('templates-multilanguage', shell.task([
// 	'gulp templates',
// 	'gulp templates --locale de'
// ]));
